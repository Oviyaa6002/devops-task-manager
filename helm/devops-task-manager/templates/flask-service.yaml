apiVersion: v1
kind: Service

metadata:
  name: flask-service

spec:
  selector:
    app: flask

  ports:
    - protocol: TCP
      port: {{ .Values.flask.servicePort }}
      targetPort: {{ .Values.flask.servicePort }}

  type: ClusterIP
